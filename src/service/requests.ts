export const backendUrl = "http://localhost:8000";
export const loginEndpoint = `${backendUrl}/login`
export const apiEndpoint = `${backendUrl}/api`;
export const beneficiariesEndpoint = `${apiEndpoint}/beneficiaries`;
